<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.canteen.mapper.AppTFlowMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.canteen.entity.StFlow">
        <id column="FlowNo" property="FlowNo" />
        <result column="Title" property="Title" />
        <result column="BusType" property="BusType" />
        <result column="AddUserNo" property="AddUserNo" />
        <result column="AddTime" property="AddTime" />
        <result column="ApproStatus" property="ApproStatus" />
    </resultMap>
    <update id="updateStatus"  parameterType="java.util.Map" >
      update app_t_flow set ApproStatus =#{staus} where FlowNo = #{id}
    </update>

    <insert id="insert" parameterType="com.canteen.entity.StFlow">
		INSERT into
		app_t_flow (FlowNo,Title,BusType,AddUserNo,AddTime,ApproStatus )
		values (#{FlowNo},#{Title},#{BusType},#{AddUserNo},#{AddTime},#{ApproStatus} )
	</insert>
</mapper>
